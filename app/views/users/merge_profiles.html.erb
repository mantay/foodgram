<% provide(:title, "Объединить профили") %>
<%= render('layouts/aside_menu') %>
<div class="col-md-6">
  <h3>Объединить профили</h3>
  <p>Вы можете объединить свои профили социальных сетей в один аккаунт.</p>
  <p>Добавленные профили:</p>
    <ul>
      <% if @social_profiles %>
        <% @social_profiles.each do |profile| %>
          <li class="text-success"><%= profile.provider %></li>
          <% @all_social_profiles.delete(profile.provider) %>
        <% end %>
      <% else %>
        <li>
          <i>Вы не интегрировали ни одного профиля.</i>
        </li>
    <% end %>
  </ul>
  
  <% unless @all_social_profiles.empty? %>
    <p>Вы можете интегрировать профили:</p>
    <ul>
      <% @all_social_profiles.each do |profile| %>
        <li><%= link_to profile, user_omniauth_authorize_path(profile) %></li>
      <% end %>
    </ul>
  <% end %>

</div>
